# summary

Watch for changes and rebuild Tailwind CSS automatically.

# description

Watches LWC and source CSS files for changes. On each change, recompiles Tailwind CSS and regenerates per-component CSS files. Does not deploy — use `sf project deploy start` separately.

# examples

- Start watching:

  <%= config.bin %> <%= command.id %>
